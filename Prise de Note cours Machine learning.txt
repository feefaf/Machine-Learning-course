UCI ML repo:
contient un max de dataset sur lesquels on peut s'entrainer.

Exemple : MAGIC Gamma Telescope Data Set
un telescope regroupe des infos sur des particules d'énergies. et faut prédire quel type de particule créer ce type de radiation.
Faut checker les patterns 


Cours sur le machine learning :

Different type de classification
- Supervised, unsupervised
- binary
- one hot encoding
- gradual

Types de prédiction:
 - Classification : prédire la catégorie de l'individu (donnée qualitative)
- Regression : prédire la valeur continue de l'individu. (donnée quantitative)


Loss Functions :
-----------------
L1 Loss
L2 Loss (lisse la courbe. si la perte est trop grande c'est très pénalisé et inversement, le millieu est lissé)
Binary Cross-entropy Loss : meilleure sont les performance plus la loss descend
Accuracy : % de vrai réponse.



Tips:
- Scale the feature datas
- C'est mieux d'avoir autant d'individu pour chaque catégorie quand on entraine un modèle


Le machine learning ça fonctionne plus ou moins toujours de la même façon:
- On créer le modèle avec ses hyperparamètres
- On fit le modèle a un jeu de donnée.
-Avec ce modèle fit on peut faire plein de choses, prédire, faire des analyses de qualité.

Contexte metrique:
- les positifs
- les négatifs
- les prédis positifs
- les prédis négatifs
- les vrai positifs (label comme positifs et le sont vrm)
- les faux négatifs (label comme négatifs mais positifs)
- les faux positifs (label comme positifs mais négatifs)
- les vrai négatifs (label comme négatifs et le sont vrm.
en théorie:
- vrai = le modèle a bien prédis
- faux = le modèle a mal prédis.
- donc vrai xx = prédis xx et est xx
- et faux xx = prédis xx mais est yy
Métriques:
- Précision : pourcentage de vrai positifs sur les prédis positifs (100% si, tout ceux qu'on a predis positifs le sont)
- Recall : pourcentage de vrai positifs sur les positifs (100% si, tout les positifs sont prédis positif) 
- Score F1 : combinaison des 2 scores. 
- Accuracy : le pourcentage de points correctement prédis.


Naive bayes:
--------------
Particulièrement efficace avec des données textuelles.
exemple, pour filtrer les emails spam de ceux qui sont des messages normaux.
pourquoi naif ? parce que bayes suppose de base que tout les paramètres sont indépendant.

C'est essentiellement un modèle basé sur des probabilité conditionnelles. Donc, un puissant modèle de classification, très peu complexe.

Hyperparamètres : y'en a très peu.
Quand utiliser naive bayes ?:
- détection de spam
- classification de documents
- catégorisation d'emails. etc
- analyse textuelle quoi
- c'est un modèle peu complexe et rapide
Quand éviter:
- Quand les variables sont fortements liées et dépendantes entre elles.

modèles:
- Gaussian NB pour valeurs numériques
- Multinomial NB pour valeurs textuels.


Regression logistique :
------------------------
Une regression linéaire permet la prédictions de leurs continus sur une ligne.
Lorsque la cible est une valeur catégorielle. Cela pose problème.
comme par exemple prédire une réussite/un echec.
Pour cela, nous n'avons pas besoin d'une valeur continue, nous avons besoins d'une probabilité.
"conformément aux valeurs d'entré, quelle est la probabilité de réussite"
c'est la que la régression logistique est parfaite.
Grâce a l'utilisation de la fonction sigmoid et a sa forme particulière.
Les prédictions passent d'une range de - infini + infini a une range de 0 a 1, avec une asymptote en haut et en bas. un résultat parfaitement interprétable comme une probabilité.

une méthode de déscente de gradient est utilisée pour optimiser la foncition de départ a passer dans le sigmoid.




log loss, ou binary cross-entropy.

Support Vector Machines (SVM) :
--------------------------------
Le but est de tracer une ligne sur le jeu de donnée qui divise les individus par catégorie. en 3D c'est un plan.
ça fonctionne mal quand les data ont des outliers.
kernel trick : x -> (x, x²)


Reseaux de neurones :
---------------------
Qu'est ce qu'un neurone.
- d'abord nous avons nos paramètres d'entrée
- chacun de ces paramètres sera multiplié far un facteur w qui servira a ponderer la valeur.
- le résultat, la somme de ce calcul finit dans un neurone
- un biais peut être ajouté a cette somme
- le résultat du neurone ira dans une "fonction d'activation"
- La fonction d'activation génèrera un output.
- Voila comment fonctionne un neurone

Toute la magie du réseau de neurone est la fonction d'activation. Si elle n'est pas présente, le réseau de neurone ,n'est qu'un modèle linéaire.

Qu'est ce qu'une fonction d'activation ?:
Ce sont des fonction non linéaires. 
Le sigmoid par exemple est une fonction d'activation. Ces dernières transforment les fonctions linéaires en score de probabilité.
- Sigmoid
- Tanh
- ReLU

Comment le modèle s'entraine ?
Après être passé par le modèle, le résultat est passé dans la square loss function qui va calculer sa performance.
le but va être, grâce a la descente de gradient de minimiser la loss en corrigeant tout les poids w du réseaux de neurone.
le réseau de neurone possède un paramètre alpha, aussi appelé "learning rate" qui sera utilisé dans le calcul du nouveau poids afin de définir a quel point ce dernier sera corrigé conformément a la direction que demande de prendre le résultat de la déscente de gradient.

attention a l'overfitting, faut pas que le nombre de epochs soit trop elevé car sinon il apprendra bien trop a se perfectionner sur le modèle d'entrainement.


TensorFlow est la librairie de choix pour ça

Qu'est ce qu'un optimizer et qui est Adam ?



Fonctionnement de Pytorch :
---------------------------
- D'abord, on converti X et y en tenseurs torch.
- avec ces tenseurs ont les rassemble en 1 dataset.
- pour les préparer a l'entrainement dans le modèle, on utilise un dataloader qui les divises en batchs





